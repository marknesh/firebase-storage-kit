{
  "name": "firebase-storage-kit-workspace",
  "type": "module",
  "private": true,
  "packageManager": "bun@1.3.13",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "clean": "turbo run clean",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "tsup": "^8.5.1",
    "turbo": "^2.9.12"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  }
}
