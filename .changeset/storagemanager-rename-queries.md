---
"firebase-storage-kit": major
---

Rename `UploadManager` to `StorageManager` and `UploadState` to `StorageState`. Add `exists`, `getMetadata`, `getDownloadURL`, and `delete` on `StorageManager` and `StorageProvider`.
