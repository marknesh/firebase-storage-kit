---
"firebase-storage-kit": minor
---

Add React hooks via `firebase-storage-kit/react`: `useStorageManager`, `useStorageState`, `useUpload`, `useBatch`, and optional `StorageManagerProvider`. React is an optional peer dependency.
